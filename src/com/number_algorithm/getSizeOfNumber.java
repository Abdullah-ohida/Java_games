package com.number_algorithm;

public class getSizeOfNumber {
    public static int getSize(int number) {
        String numberToString = Integer.toString(number);
        return numberToString.length();
    }
}
